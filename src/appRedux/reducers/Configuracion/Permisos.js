import {
    
} from "constants/SistemaTypes"

const INIT_STATE = {
    cargandoPermisosTipoUsuario : true
}

export default (state = INIT_STATE, action) => {
    switch (action.type) {

        
        default: 
            return state
    }
}